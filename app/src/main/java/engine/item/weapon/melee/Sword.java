package engine.item.weapon.melee;

public class Sword extends MeleeWeapon {
    public Sword(String name, String description, int weight, int damage, int speed, int nearRangeBonus, int maxRange) {
        super(name, description, weight, damage, speed, nearRangeBonus, maxRange);
    }
}
